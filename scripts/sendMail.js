function SendMail() {
    let params = {
        from_name: "Company Name",
        username: document.getElementById(),
    }

    emailjs.send(service_cwpkviv, template_rdzi99l, params).then(alert);
    // is .then() just a short version of if(0)? 
}